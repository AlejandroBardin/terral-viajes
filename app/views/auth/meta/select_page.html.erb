<div class="min-h-screen flex items-center justify-center bg-background-dark p-4">
  <div class="w-full max-w-md bg-surface-dark border border-slate-700/50 rounded-2xl shadow-2xl overflow-hidden animate-in fade-in zoom-in-95 duration-200">
    <div class="p-6 border-b border-slate-700/50 text-center">
      <div class="mx-auto w-12 h-12 rounded-full bg-[#1877F2]/10 flex items-center justify-center text-[#1877F2] mb-3">
        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
      </div>
      <h2 class="text-xl font-bold text-white">Selecciona una Página</h2>
      <p class="text-slate-400 text-sm mt-1">Elige la página de Facebook para sincronizar leads.</p>
    </div>

    <div class="p-4 max-h-[400px] overflow-y-auto space-y-2">
      <% if @pages.any? %>
        <% @pages.each do |page| %>
          <%= button_to auth_meta_save_page_path(page_id: page['id']), class: "w-full flex items-center gap-4 p-3 rounded-xl hover:bg-slate-700/50 transition-colors group text-left" do %>
            <% if page.dig('picture', 'data', 'url') %>
              <img src="<%= page.dig('picture', 'data', 'url') %>" class="w-10 h-10 rounded-full bg-slate-700" alt="">
            <% else %>
              <div class="w-10 h-10 rounded-full bg-slate-700 flex items-center justify-center text-slate-400">
                <span class="material-symbols-outlined">flag</span>
              </div>
            <% end %>

            <div class="flex-1">
              <h3 class="text-white font-medium group-hover:text-primary transition-colors"><%= page['name'] %></h3>
              <p class="text-xs text-slate-500">ID: <%= page['id'] %></p>
            </div>

            <span class="material-symbols-outlined text-slate-600 group-hover:text-primary">arrow_forward_ios</span>
          <% end %>
        <% end %>
      <% else %>
        <div class="text-center py-8">
          <p class="text-slate-400 mb-4">No encontramos páginas donde seas Administrador.</p>
          <a href="#" class="text-primary hover:underline text-sm">¿Ayuda?</a>
        </div>
      <% end %>
    </div>

    <div class="p-4 bg-slate-800/30 border-t border-slate-700/50 text-center">
      <%= link_to "Cancelar", admin_settings_path, class: "text-sm text-slate-400 hover:text-white transition-colors" %>
    </div>
  </div>
</div>
